{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm --prefix desktop install && npm --prefix desktop run build",
  "outputDirectory": "desktop/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
